import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#fff',
    borderColor: '#000',
    borderRadius: 5,
    borderWidth: 1,
    justifyContent: 'center',
    padding: 16,
  },


  question: {
    fontSize: 24,
    marginBottom: 16,
  },

  toggleAnswer: {
    height: 42,
    alignItems: 'center',
    justifyContent: 'center',
  },

  answer: {
    fontSize: 24,
    marginTop: 16,
  },
});
