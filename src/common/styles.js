import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    padding: 12,
    borderColor: 'black',
    fontSize: 18,
    borderWidth: 1,
    borderRadius: 6,
  },
});
